{% extends 'base.html.twig' %}

{% block title %}Hello AccountController!{% endblock %}

{% block body %}

<div class="example-wrapper">
    <div>
        <button><a href="{{ path('app_account', {'id': 'compte'}) }}">Compte</a></button>
        <button><a href="{{ path('app_account', {'id': 'commande'}) }}">Commandes</a></button>
    </div>
    {% if id == 'compte' %}
        {% include "account/_user_account.html.twig" %}
    {% elseif id == 'commande' %}
        {% include "account/_commande.html.twig" %}
    {% endif %}

</div>
{% endblock %}
